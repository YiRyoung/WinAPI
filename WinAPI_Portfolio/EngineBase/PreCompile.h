#pragma once

// 미리 컴파일할 헤더들.
// 미리 컴파일된 헤더 사용 시 .pch 파일에 빌드결과가 저장된 후 다시 빌드되지 않는다.
// -> 컴파일 시간 단축

// os
#include <Windows.h>

// std
#include <list>
#include <vector>
#include <map>
#include <string>
#include <iostream>
#include <functional>
